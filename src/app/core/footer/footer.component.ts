import { Component } from '@angular/core';

/**
 * Represents the application's footer section.
 * Typically contains contact info, copyright,
 * or external links.
 */
@Component({
  selector: 'app-footer',
  templateUrl: './footer.component.html',
  styleUrl: './footer.component.scss'
})
export class FooterComponent {
  // Currently no logic is required for the footer.
}
